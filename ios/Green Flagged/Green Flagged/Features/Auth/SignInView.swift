import SwiftUI

// TODO Phase 2 — email/password + Google OAuth (ASWebAuthenticationSession).
struct SignInView: View {
    var body: some View {
        ZStack {
            Color.gf.bg.ignoresSafeArea()
            Wordmark()
        }
    }
}
