import SwiftUI

// TODO Phase 3 — greeting, quick actions, usage meter, recent contracts.
struct DashboardView: View {
    var body: some View {
        ZStack {
            Color.gf.bg.ignoresSafeArea()
            Text("// DASHBOARD")
                .font(.gf.label)
                .foregroundStyle(Color.gf.fg3)
        }
    }
}
