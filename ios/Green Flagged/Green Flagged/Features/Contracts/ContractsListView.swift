import SwiftUI

// TODO Phase 3 — filter chips, pull-to-refresh, direct Supabase reads.
struct ContractsListView: View {
    var body: some View {
        ZStack {
            Color.gf.bg.ignoresSafeArea()
            Text("// CONTRACTS")
                .font(.gf.label)
                .foregroundStyle(Color.gf.fg3)
        }
    }
}
