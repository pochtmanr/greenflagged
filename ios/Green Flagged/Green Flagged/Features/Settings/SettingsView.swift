import SwiftUI

// TODO Phase 3 — account / billing / about / sign-out.
struct SettingsView: View {
    var body: some View {
        ZStack {
            Color.gf.bg.ignoresSafeArea()
            Text("// SETTINGS")
                .font(.gf.label)
                .foregroundStyle(Color.gf.fg3)
        }
    }
}
